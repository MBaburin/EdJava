Создайте интерфейс Playable с методом play(). Реализуйте этот интерфейс в двух классах:
    Класс Guitar с реализацией метода play(), который выводит "Играет на гитаре".
    Класс Piano с реализацией метода play(), который выводит "Играет на пианино".
В классе Main создайте объекты этих классов и вызовите метод play() для каждого из них.