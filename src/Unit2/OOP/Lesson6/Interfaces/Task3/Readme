 Создай интерфейс Shape, который будет содержать два метода:

 area() — для вычисления площади фигуры.
 perimeter() — для вычисления периметра фигуры.

Реализуй этот интерфейс в двух классах: Circle и Rectangle. Для круга площадь вычисляется по формуле π×r2π×r2, а периметр — по формуле 2×π×r2×π×r. Для прямоугольника площадь вычисляется как длина×ширинадлина×ширина, а периметр — как 2×(длина+ширина)2×(длина+ширина).
В методе main создавай объекты этих классов и выводи на экран их площадь и периметр.